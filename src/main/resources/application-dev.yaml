server:
  port: 8081
spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

spadas:
  file:
    baseUri: ./dataset-mini/
    # 网页请求路径
    staticUri: /dataset-mini/
